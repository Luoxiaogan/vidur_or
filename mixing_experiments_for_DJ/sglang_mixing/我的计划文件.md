阅读`mixing_experiments_for_DJ`和`mixing_experiments_for_DJ/sglang_mixing/`中的的所有的python文件和markdown文件, 理解vidur and sglang, 必要时候可以阅读源代码: `sglang/` and `vidur/`

1. 可能目前的`sglang/`的修改全部都是错的, 需要我用git revert. 因此你先不写代码, 思考清楚.

2. 目前的`num_queue_reqs`的记录是有问题的, 因为我设置的rate非常大, 但是csv里面捕捉到的可能是scheduler里面瞬态的`num_queue_reqs`, 只有0或者1, `num_queue_reqs`需要阅读`sglang/`的源代码, 查找`mixing_experiments_for_DJ/sglang_mixing/test_1.py`里面的一个request被发送之后的调度过程.

3. num_queue_reqs,num_retracted_reqs,num_new_seqs,num_new_tokens,num_cached_tokens,cache_hit_rate. 可能记录都有问题, 例如你可以随机阅读`mixing_experiments_for_DJ/sglang_mixing/results/test_9.csv`的一些行.

4. 我尤其想要准确记录的是: `num_retracted_reqs`: 这次调度被retracted的req的数目, `num_new_seqs`: 这次调度新被admission的req数目.

5. num_queue_reqs 只有 0 或 1的问题我觉得不是因为Fake decode 模式下请求处理太快, 因为我设置POISSON_RATE = 1000, 说明2000个requests在2s内不发送完, 但是`test_9.csv`里面的`gpu_num_reqs`最大只是100, 并且处理了很久, 说明排队队列的长度记录肯定有问题.

6. (已知信息如下)有没有可能跟踪每个request的metric, 例如记录arrive time, schedule time, completion time.
    1. 例如一次launch server, 记录被发送到server的(并且是接收到并且completion的)request的所有时间(arrival, 第一次被schedule, completion)
    2. arrival, 第一次被schedule, completion->所有req的属性里面有的时间.
    3. 维护了一个csv, 这个里面写入被记录的req的metrics
    4. 修改代码使得记录到csv里面
    5. SGLang 已经有的 per-request 时间统计输出中, 原生的metric记录有:Arrival Time, First Schedule Time, Completion Time等. 是一个req完成之后从这个req里面的属性里面提取计算的, 因此如果能够弄明白每次操作中request的属性是如何更新的, 就能够详细记录metrics.
    6. req的属性里面记录了: decode_prealloc_queue_entry_time (请求到达 Scheduler 的时间，比客户端发送时间晚), decode_transfer_queue_entry_time (进入传输队列时间), wait_queue_entry_time (进入等待队列时间), forward_entry_time (forward_entry_time 是"即将进入GPU执行"的时间，在实际 run_batch() 之前几微秒到几毫秒。对于大多数分析来说可以认为是"第一次进入GPU的时间"), completion_time (最后一次decode后完成的时间), 每个 Req 对象有 retraction_count 属性.
    7. 请求到达server的时间: Tokenizer侧（HTTP接收时间）：state.created_time = obj.received_time，在 ReqState 中，不在 Req 中
    8. 在completion的时候(假设这个时候我记录dataframe), req的属性里面的所有的我可以访问的信息: 基本信息(暂时不需要保存内容之类的), seqlen, extend_input_len, cached_tokens, finished_reason, finished_len, time_stats.decode_prealloc_queue_entry_time, time_stats.decode_transfer_queue_entry_time, time_stats.wait_queue_entry_time, time_stats.forward_entry_time, time_stats.completion_time, retraction_count, is_retracted, retracted_stain, PD分离相关(Fake DP里面可能先记录一下吧), kv_committed_len, kv_allocated_len.
    9. ReqState 先初始化, Req后初始化; Completion时不能直接访问ReqState获取HTTP接收时间, 需要修改.

7. 我已经修改了代码, 在 Req 对象的 time_stats 中记录 HTTP 接收时间，使得在 completion 时可以访问完整的时间链.修改后数据流: HTTP接收 → tokenizer_manager (created_time) → TokenizedGenerateReqInput.received_time → scheduler → Req.time_stats.lb_entry_time; 从而Completion 时可访问req.time_stats.lb_entry_time (HTTP接收时间). 修改在: `sglang/python/sglang/srt/managers/io_struct.py. line 764~767`, `sglang/python/sglang/srt/managers/tokenizer_manager.py. line 1061`, `sglang/python/sglang/srt/managers/tokenizer_manager.py. line 1082~1083`, `sglang/python/sglang/srt/managers/scheduler.py. line 1478, 1746`, `sglang/python/sglang/srt/managers/io_struct.py, 931~932`

8. 在每一个req的completion的时候记录每一个req的metrics: seqlen, extend_input_len, cached_tokens, finished_reason, finished_len, time_stats.lb_entry_time, time_stats.decode_prealloc_queue_entry_time, time_stats.decode_transfer_queue_entry_time, time_stats.wait_queue_entry_time, time_stats.forward_entry_time, time_stats.completion_time, retraction_count, is_retracted, retracted_stain, PD分离相关(Fake DP里面可能先记录一下吧), kv_committed_len, kv_allocated_len.
    1. 按照顺序(和SGLang 已经有的 per-request 时间统计输出一起执行就好)保存在一个初始化好的df里面, 然后自动化存储到launch server的时候添加的一个csv参数里面.
    2. 这个的实现计划在`mixing_experiments_for_DJ/sglang_mixing/request_csv导出.md`
    3. 已经实现好了, 使用的方法是: --export-request-metrics-to-csv /tmp/request_metrics.csv
    4. CSV 将输出 21 列，包括lb_entry_time (epoch) - 用于绝对时间排序; lb_entry_time_perf (perf_counter) - 用于精确计算时间间隔.

使用方法:
```bash
export LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH

python -m sglang.launch_server \
    --model-path /data/pretrained_models/Qwen2.5-1.5B-Instruct \
    --port 30000 \
    --disaggregation-mode decode \
    --disaggregation-decode-enable-fake-auto \
    --export-batch-metrics-to-file mixing_experiments_for_DJ/sglang_mixing/results_batch/batch_metrics_6.csv \
    --export-request-metrics-to-csv /home/lg/vidur_or/mixing_experiments_for_DJ/sglang_mixing/results_req/request_metrics_6.csv
```

9. 我开始进行测试:
    1. 用`mixing_experiments_for_DJ/sglang_mixing/test_2.py`并行同时把1000个打过去, 发现`mixing_experiments_for_DJ/sglang_mixing/results_batch/batch_metrics_2.csv`里面batchsize=1000.
    2. `test_1.py`最开始因为aiohttp 默认 TCPConnector(limit=100)



9. 思路是: 如果可以记录req的所有相关的metrics到csv里面供我分析, 那么在测试之后直接分析这个csv. 
    1. 对于任何一个时刻, 可以计算所有 (HTTP 接收时间>这个时刻) && ()