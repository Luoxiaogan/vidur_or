# Symbols

- `\lambda`: arrival rate / QPS in experiments
- `\lambda^*`: stability boundary
- `n_j`: per-type WAIT threshold
- `n_k`: per-segment Nested WAIT threshold
- `\mathrm{tl}`: total in-flight limit
- `M^*`: equilibrium memory level required for stability at the boundary of the stability region
