# Batch 指标跟踪需求

## 需求

对于每一个 iteration 处理的 batch，跟踪以下两个量的**时间序列**（横轴 batch index，纵轴值）。

## 定义

### 1. Batch Total KV Tokens (需新增)

一个 batch 中所有 request 的当前序列长度之和（即 KV cache 占用）。

```python
batch_total_kv_tokens = sum(request.num_processed_tokens for request in batch.requests)
```

- 相关代码: [`request.num_processed_tokens`](vidur/entities/request.py#L169-L170)

### 2. Batch Size (已有)

一个 batch 中的 request 数目。

```python
batch_size = batch.size  # len(batch.requests)
```

- 相关代码: [`batch.size`](vidur/entities/batch.py#L99-L100)
- 已有指标: `BATCH_SIZE` ([metrics_store.py L701-703](vidur/metrics/metrics_store.py#L701-L703))

## 目标输出

| 指标 | CSV 文件 | 图表类型 |
|------|----------|----------|
| Batch Total KV Tokens | `batch_metrics.csv` | 时间序列 (batch index vs value) |
| Batch Size | `batch_metrics.csv` | 时间序列 (batch index vs value) |

需启用配置: `keep_individual_batch_metrics=True`
