## 2026-03-01

1. `比较测试_3_1_baseline_use_defaults`:
    1. 使用: `Compare_config.py` and `Compare_run.py`
    2. Single Type, prefill = 630, decode = 20, max_tokens = 650, num_requests = 10000.
    3. Rates = [14, 15, 16, 17, 18, 19, 20, 21]
    4. wait, total_limit = 725, 测试 small threshold 的时候, 我使用了 total_limit = 2.
    5. 测试非PD分离, 希望 underloaded rate 下用 small threshold, 得到更小的latency; 然后在另一个实验里面拉大 num_requests, 凸显 vllm and sarathi 在 overloaded 的时候的 latency blow up, with wait hasing a wider range of stable region.
    6. 参考: `new_experiments_for_revision/exp_1_wait/实验复现文件夹_参数存储/Compare_config_PD分离.py`
    7. 注意 vllm 有 default 和 unlimited 两种 settings, 其实 sarathi 也有两种 settings, 但是就是看需不需要写出来了. 
    8. 使用的是: `general_nested_booking_limit_replica_scheduler.py`
    9. batchsize 定死是 total_limit 的大小, 然后 admission 的 threshold 是 ceil(total_limit/decode)

    10. 目前用 grid search 可以把 wait 在不同的 arrval rate 下的"大概"最好的 threshold 找到, 让 latency 不要太难看
    11. (rate, total_limit) 大概的对应关系是: (14, 20), (15, 40), (16, )

2.  `PD分离实验3_统一测试_vllm_default`
    1. PD 分离的情况下, 由于耗时最多的 prefill 已经完成, 那么 underloaded rate 的临界点会很大
    2. wait 大概在 qps = 400 的时候才blow up, 但是 vllm 早在 qps = 350 早就已经 blow up 了.
    3. 所以需要测试 vllm 在更小的 rate 下什么时候稳定, 预计这个时候的 latency 应该也极为小. 因此这里遇到了一个需要测试的问题, 例如 vllm 在 qps <= 300 的时候稳定, 那么我们预计 wait 在 qps <= 300 的时候大概也是让 threshold 基本 = 0 得到最小的 latency (throughput 基本不变), 但是 qps \in [300, 400] 的时候, wait 我认为不能用 threshold 基本 = 0, 因此这个会近似成 vllm 所以炸掉, 所以需要测试.
    4. wait, total_limit = 720, num_requests = 50000.
    5. 参考: `new_experiments_for_revision/exp_1_wait/实验复现文件夹_参数存储/PD_compare_config.py`
    6. 先测试标准 wait 和 vllm in [100, 150, 200, 250, 350, 360, 370, 380, 390, 400, 410, 420, 430, 440, 500]
    7. 然后再对 wait 做 (arrival rates) x (thrshold) 的 grid search.

3. 对 d0, d1的线性回归.

4. 在 paper 里面写清楚 M 的计算方式, 以及 M/(l_1 \bar{l}) 的计算和真实的最大 total limit 大致对应 (725 和 720.)

5. 然后就是 multi type 的实验. 这里的 2-types and 3-types 就只需要调出来 wait 比 baseline 好就可以了, 然后真实数据也是就两张图.